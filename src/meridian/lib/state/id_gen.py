"""Re-export shim -- contents merged into spawn_store.py."""

from meridian.lib.state.spawn_store import (  # noqa: F401
    next_chat_id as next_chat_id,
    next_space_id as next_space_id,
    next_spawn_id as next_spawn_id,
)
