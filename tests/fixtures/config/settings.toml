[defaults]
max_depth = 7
max_retries = 4
retry_backoff_seconds = 0.5
primary_agent = "lead-primary"
agent = "worker-agent"

[timeouts]
kill_grace_seconds = 1.5
guardrail_seconds = 45.0
wait_seconds = 900.0

[permissions]
default_tier = "space-write"

[primary]
autocompact_pct = 61
permission_tier = "space-write"
